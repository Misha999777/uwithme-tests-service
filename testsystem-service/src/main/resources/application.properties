#
server.port=8090
server.error.include-message=always
#
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL10Dialect
spring.jpa.hibernate.ddl-auto=update
#
spring.datasource.url=${TESTSYSTEM_JDBC_URL:jdbc:postgresql://localhost:5432/testsystem}
spring.datasource.username=${TESTSYSTEM_JDBC_USERNAME:postgres}
spring.datasource.password=${TESTSYSTEM_JDBC_PASSWORD:postgres}
#
keycloak.auth-server-url=${UNIBOT_KEYCLOAK_HOST:http://127.0.0.1:8080/auth}
keycloak.realm=${UNIBOT_KEYCLOAK_REALM:tCoMaD}
keycloak.resource=${UNIBOT_KEYCLOAK_CLIENT:testsystem}
keycloak.credentials.secret=${UNIBOT_KEYCLOAK_SECRET:J1CIt0yVlSSVLKj3711o1Uc6KU2GGeuk}
